$("input.maxlength-input").maxlength({
  alwaysShow: true,
  placement: "top",
  validate: true,
  warningClass: "small form-text text-success",
  limitReachedClass: "small form-text text-danger",
});
